def load_camera_config(camera_id: str):
    """
    Manual camera config is intentionally disabled.
    The system always uses auto-calibration so it can handle unknown CCTV footage.
    """
    return None